import 'package:flutter/material.dart';

class EtudeSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("设置"),
      ),
      body: Text("设置页"),
    );
  }
}
